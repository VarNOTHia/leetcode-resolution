/**
 * @param {number[]} asteroids
 * @return {number[]}
 */
var asteroidCollision = function(asteroids) {
  const stack = [];
  for(const atser of asteroids){
    let isPush = true;
    while(isPush){
      
    }
  }
  return stack;
};