/** LEETCODE 202. 快乐数（hashset 存储，再次出现就判断存在）
 * @param {number} n
 * @return {boolean}
 */
var isHappy = function(n) {
  while(1){
    let arr = n.toString().split('');
    for(let i = 0; i < arr.length; i++){
      
    }
  }
};