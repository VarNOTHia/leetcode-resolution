/** 1466. 重新规划路线
 * @param {number} n
 * @param {number[][]} connections
 * @return {number}
 */
var minReorder = function(n, connections) {
  // 因为 0 本身可以和所有节点连通，所以这道题本质上只需要计节点就可以了。
  const vertexes = connections.length;
  

};